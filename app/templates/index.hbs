<!-- Index page -->
{{#jqm-page}}
  {{#jqm-header position="fixed"}}
      <h1>Ember app</h1>
  {{/jqm-header}}
  {{#jqm-content}}
      <div align="center">
        {{jqm-button action=(action "changePage" "home") text="To home page"}}
        {{jqm-button action=(action "changePage" "map") text="To map page"}}
        {{jqm-button action=(action "changePage" "login") text="To login page"}}
      </div>
  {{/jqm-content}}
{{/jqm-page}}
