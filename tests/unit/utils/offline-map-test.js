import offlineMap from '../../../utils/offline-map';
import { module, test } from 'qunit';

module('Unit | Utility | offline map');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = offlineMap();
  assert.ok(result);
});
